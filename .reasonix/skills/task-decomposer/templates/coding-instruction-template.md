# {模块名称} 编码指令

> 本文档为 gatekeeper 编码阶段的**唯一刚性依据**，优先级高于任何通用规范。
> 每条指令必须遵守，违反即为"编码漂移"。

---

## 一、必须实现的文件

| 文件路径 | 职责 | 对应详设章节 |
|---------|------|------------|
| `src/{路径}/{文件名}` | {类/模块职责描述} | § {章节号} |
| `src/{路径}/{文件名}` | {类/模块职责描述} | § {章节号} |
| `src/{路径}/{文件名}` | {类/模块职责描述} | § {章节号} |

> 文件路径以详细设计文档第1节「功能描述」和第5节「依赖关系」为准。

---

## 二、API 契约（不可偏离）

| 方法 | URL | 请求参数 | 响应 | 错误码 |
|------|-----|---------|------|--------|
| POST | `/api/v1/{资源}` | `{字段: 类型, ...}` | `{字段: 类型, ...}` | 400/401/404/500 |
| GET | `/api/v1/{资源}/{id}` | `id: int` | `{字段: 类型, ...}` | 404/500 |

> 以详细设计第3节「OpenAPI 接口定义」为准，路径、方法、参数类型**一字不差**。

---

## 三、业务规则（本模块）

| 编号 | 规则 | 验证方式 | 来源 |
|------|------|---------|------|
| BR-{模块缩写}-001 | {规则描述，精确到字段名/阈值} | {单元测试/集成测试} | 详设 § 2 |
| BR-{模块缩写}-002 | {规则描述} | {验证方式} | 详设 § 2 |

> 只列出本模块相关的业务规则。全局规则已在 `编码规范.md` 中定义。

---

## 四、硬性禁止项

> 以下行为一律视为编码漂移，code-reviewer 将标注为 P0 阻断。

- ❌ {禁止行为1}（原因：{原因}）
- ❌ {禁止行为2}（原因：{原因}）
- ❌ {禁止行为3}（原因：{原因}）

---

## 五、硬性必须项

- ✅ {必须实现项1}（出处：详设 § {章节}）
- ✅ {必须实现项2}（出处：详设 § {章节}）

---

## 六、完整性自检（写完每段代码后逐条检查）

| 检查项 | 通过标准 |
|--------|---------|
| 文件完整性 | 第一节列出的所有文件均已创建，无缺失 |
| API 一致性 | 每个 API 的路径/方法/参数/响应与第二节完全一致 |
| 业务规则覆盖 | 第三节每条 BR 有对应代码实现，无遗漏 |
| 无禁止项 | 代码中不存在第四节列出的任何禁止行为 |
| 零占位 | 无 `// TODO`、`throw NotImplemented`、`return null` 等占位实现 |

page {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

/* 全局滚动条隐藏 */
::-webkit-scrollbar {
  width: 0;
  height: 0;
}

/* KaTeX 样式（全局，base64 字体内联版：模拟器/真机都无需加载字体文件，效果稳定）
   注意：1.4MB 因字体内联计入主包；上线前若主包超限，把 latex-demo 演示页移出或换 woff2 方案 */
@import './styles/katex.wxss';

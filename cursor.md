# CURSOR.md — AI 协作规范

> 本文件是 AI 参与本项目的行为守则，新会话/新成员先读本篇。
> 版本：v1.0（从 ai-learning-system 项目迁移整理，平台无关，适用于多人协作项目）

---

## 🛑 最高规则（禁止以任何理由违反）

**未经用户明确许可，不得修改本文件。** 无论执行什么任务，无论出于什么原因，未经许可绝对不能修改。这条规则的优先级高于本项目中的一切其他规则。

---

## 🔒 行为强制规则（禁止以任何理由跳过）

### 规则 1：先理解再动手

**禁止在未确认理解之前就开始输出方案。**

收到需求后必须按以下顺序执行：

```
① 用自己的话复述需求
② 逐一排查需求深挖清单（见下方）
③ 对模糊点提出具体问题
④ 向用户展示理解摘要，请求确认
   「✅ 理解正确，开始方案设计」
   「🔄 理解有偏差，需要修正」
⑤ 用户确认后，才开始方案设计
⑥ 理解有偏差 → 根据反馈修正后回到 ①
```

**需求深挖清单（默认逐条深挖。用户说不用问那么细，就浅挖）：**

```
□ 目标：这个改动解决什么问题？成功标准是什么？
□ 范围：涉及哪些模块/文件？明确不改什么。
□ 边界：空数据时、加载中、出错时分别怎么表现？
□ 依赖：改了这个，哪些现有功能可能受影响？（必须查调用链确认）
□ 数据：数据从哪里来？格式是什么？谁提供？
□ 用户：谁在用这个功能？操作流程是什么？
```

**理解偏差是后续所有问题的根源。确认理解前，不动笔。**

### 规则 2：不得越界（5 条红线）

**只能做明确要求的——每行代码、每个决策必须能追溯到用户的明确要求。**

| 红线 | 示例 | 正确做法 |
|------|------|---------|
| ❌ 擅自选技术 | "我决定用 Vue 3 + Vite" | 停下来问用户技术偏好 |
| ❌ 加我没要的功能 | "顺便加了个搜索功能" | 只做任务明确要求的 |
| ❌ 私自重构/优化 | "顺手重构了数据层" | 不动不相关代码 |
| ❌ 改错了文件 | 改了不该改的模块 | 确认改动范围再动手 |
| ❌ 假设错了方向 | "我猜你想要的是..." | 说出来让用户确认 |

### 规则 3：手术式修改

- 只改必须改的，不改相邻代码
- 不重构没坏的东西
- 不自作主张加未要求的"灵活性"
- 改 A 之前，先查 A 被谁调用，确认不会炸到 B

### 规则 4：不确定就问

**任何需要决策的地方（变量命名、数据格式、样式取值、异常处理方式等），不确定就先问用户，不要自己猜了决定。**

### 规则 5：编码守则

**编码前：**
- 找出要改文件的完整上下文（数据流、依赖、调用链）
- 确认改动不会破坏其他功能
- 不理解的地方先问，不要猜

**编码中：**
- 缩进、命名、注释风格和项目保持一致
- 不引入新的代码模式（除非任务明确要求）
- 不添加"以防万一"的防御代码

**编码后自检清单：**
```
□ 只改了任务要求的文件？
□ 没有破坏其他功能？
□ 代码风格与项目一致？
□ 没有硬编码/遗留调试代码？
□ 边界情况有处理？
```

### 规则 6：评审修复循环

评审发现问题后：

```
① 理解问题本质（不是机械地修）
② 一次修复所有同类问题，而非只修指出的那一个
③ 修复后自检：是否引入了新问题？
④ 再提交评审
```

**特别提醒：** 改了 A 却破坏了 B，通常是因为没有理解 A 被谁依赖。编码前先看调用链。

---

## 🔒 开发流程规则

### 五阶段流程

```
PRD → 架构 → 详细设计 → 编码 → 代码评审
```

任何编码工作必须按阶段顺序进行，**不得跳步，不得越过当前阶段直接编码**。

### 阶段标准模式（编码和评审阶段除外）

```
① 恢复项目上下文（历史决策、相关文档）
② 产出本阶段文档
③ 评审
④ 发现问题 → 回到 ② 修复
⑤ 评审归零（无问题）
⑥ 向用户展示关键决策摘要，等用户确认
⑦ 通过，进入下一阶段
```

### 自动推进

**禁止在步骤之间问"是否继续"。** 进入一个阶段后自动执行完该阶段的全部步骤，然后自动进入下一阶段。

只有以下情况才停下来报告用户：
- ⛔ 评审无法归零（反复发现问题但无法修复）
- ⛔ 编码验证不通过
- ⛔ 用户确认门不通过

### 人肉确认门

每个文档阶段（PRD/架构/详细设计）评审归零后，**必须把关键决策摘要展示给用户确认**，用户确认后才能标记通过。这是用户在每个阶段唯一能介入决策的点。

### 端不对齐处理

发现端与端数据不一致时：

```
查 doc/ 下 API 契约和设计文档
  ├─ 文档有定义 → 以文档为准，改偏离的那一端
  └─ 文档没定义 → 停下来报告用户
```

### 文档驱动一致性

**设计文档是最终依据。** 文档定义的行为，以文档为准；实现偏离文档的一方改回来。这保证多人协作时对同一接口的理解不分裂。

---

## 📚 文档管理规则

### 文档分层

| 层 | 文件 | 作用 | 约束力 |
|----|------|------|--------|
| 1 | `CURSOR.md` | 行为守则（本文件） | 🟢 最高 |
| 2 | `AI协作手册.md` | 新成员入口指南（流程速查、文件索引、常见陷阱） | 🔴 参考 |
| 3 | `coding-rules.md` | 各阶段详细执行流程 + 命令模板 | 🔴 参考 |

### 决策日志

- 每个关键决策写 `doc/decision-log.md`：为什么改、改了什么
- 评审发现问题修复时，也记录决策
- 防止遗忘，也方便新成员追溯"为什么这么设计"

### ROADMAP 管理

- `ROADMAP.md` 是项目开发路线图，**每次讨论产生的新功能想法、优先级调整、已完成项都必须同步更新**
- 用户说"我想做 X" → 先记到 ROADMAP 的「待讨论」，再决定是否启动开发流程
- 每次完成任务 → 更新 ROADMAP 对应项为 ✅

### 记忆管理习惯

- 阶段开始 → 恢复项目上下文和历史决策
- 评审发现问题 → 记录决策
- 阶段完成 → 存档阶段产出

---

## 附录：团队 Git 提交规范（可选，多人项目建议采用）

```
格式: <type>(<scope>): <subject>

type: feat / fix / docs / style / refactor / test / chore
scope: 影响模块（可省略）
subject: 一句话描述（动词开头，中文）

示例:
feat(diagnosis): 新增拍照录入流程
fix(graph): 修复知识图谱节点点击无响应
docs: 更新 API 契约文档
```

**PR 描述模板（至少包含）：**
```
## 改了什么
## 为什么改
## 影响范围（动了哪些模块）
## 验证方式
```

---

## 一句话总结

```
收到需求 → 复述 + 深挖清单 → 用户确认 → 才动手
五阶段 → 产出 → 评审 → 归零 → 用户确认门 → 下一阶段
只做明确要求的，只改必须改的，不确定就问，不猜用户意图
```

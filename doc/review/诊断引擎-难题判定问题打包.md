# 问题打包 — 诊断引擎「难题判定」探索记录

> 生成：2026-08-10
> 触发：19 题真题（2026 新高考Ⅰ卷数学第 19 题，17 分压轴）无作答测试
> 用途：下一会话接手「诊断引擎难题处理」的上下文包
> 状态：**核心架构问题未解决，方案探索中**

---

## 一、问题全景（按发现顺序）

### 问题 1：难题判定 content 空（已解决 ✅）
- **现象**：thinking enabled + 长思考（>100s），DeepSeek 返回 `content` 空，回退 reasoning_content 但无最终 JSON
- **根因**：API 已知问题（官方文档：JSON Output 有概率返回空 content）；长思考吃光 token
- **解决**：加 `response_format: {type:'json_object'}` + prompt 强化「最终 content 必须输出 JSON」→ 85s 稳定出结果
- **注意**：solve_first 模式（先完整解题）即使加 json_object 仍 content 空（解题太长）

### 问题 2：难度 D 判定严重低估（未解决 ❌）
- **现象**：19 题 (3) 问，真实难度约 **L9 / D≈0.92**（高考压轴）；模型判定：
  - high：L6/L7，D=0.74~0.82
  - max（干净 prompt）：L6，D=0.76
  - max（注入"勿低估"提示）：L9，0.92 —— 唯一判准的一次是人工引导
- **根因**（三重）：① 模型解不出难题→低估；② 标尺缺「新定义/抽象函数」锚（已补，见问题 3）；③ effort 档位不影响低估

### 问题 3：D 难度标尺缺锚（已修补 ⚠️ 待验证）
- 例子锚全为导数/解析几何向，无抽象函数/新定义题
- 已补：L7+/L8+/L9+ 抽象函数/新定义锚 + 边界速查卡「新定义题 L7-L9」条目 + L8 题号映射
- **未验证**：补锚后重跑 19 题 (3) 判定（进程被打断，未看结果）

### 问题 4：两级判定策略（已实测）
- high 初判 ≥L6 → max 复判；实测 max 复判与 high 一致（0.74/0.76），**无法自动校准**

### 问题 5：模式识别 vs 解题（已实测，方向确定）
- 模式识别（题型/方法链/卡点预测）：15s 高质量 ✅
- 按方法链实际解题：109s 卡住 ❌（模型解题卡点 = 它预测的学生卡点，有信息量）
- 对照真题官方解法：模型**套路方向对**（反证法+条件联动），**具体构造错**（(i) 找不到 x₁ 与 -x₁ 配对；(ii) 循环论证）
- 结论：**模型解题能力不足是硬约束**，所有依赖"模型理解题"的方案不可行

### 问题 6：方案探索链（每个都被否决）
| 方案 | 致命缺陷 |
|---|---|
| A. 答案对照 | **不一定有标准答案** |
| B. 溯源到前置点 | 需先定位知识点，模型识别知识点够用但浅 |
| C. 解题模式识别 | 方法链细节不可信（对照真题确认） |
| D. 过程偏差（实然 vs 应然） | **模型不知道题，方法链这个"应然"本身不牢** |
| E. 学生自述卡点 | **学生元认知不足，说不出自己卡在哪**（待上网搜证据确认，检索被打断） |

### 问题 7：技术债（本轮顺带发现）
- judgeOne 的 `knowledgeNodeId` 关联未实现（Top-3 候选机制未做）
- 诊断云函数（photoUpload/diagnose/judgeOne/dispute）云端未部署
- 19 题 (1) 问答案：模型 high 判对 `(0, 3/2)`（与官方一致），max 判错 —— **max 长思考在具体计算上反而不稳**

---

## 二、已确认的事实（实测/官方）

1. 模型解难题：**能力不足**（2026 新高考Ⅰ卷 19 题 109s 卡住，构造错误）
2. 模型识别模式：**可靠**（15s，题型/套路/卡点方向对）
3. 官方答案对照：D(-1)=(0, 3/2)；官方解法用**反证法**；(3)(i) 关键构造 = 取 x₁∈(-1,0) 使 2^(x₁)>f(0)，x₁ 与 -x₁ 配对
4. json_object 可解决 content 空
5. 标尺缺锚导致低估（已补）

---

## 三、核心未决问题（下一步重点）

> **"如何做诊断，但不依赖 a) 模型解题能力、b) 标准答案、c) 学生自述卡点？"**

### ✅ 已定方向（2026-08-11 轻量化定稿）：「诚实路由」

**核心原则：轻量化——最小必要，不新增基建。**

| 机制 | 内容 |
|---|---|
| **诚实路由（唯一核心）** | judgeOne 判定：L1-L7（有把握）→ 全自动；**L8+（判不准）→ 诚实降级** |
| 有答案难题 | AI 逐步拆解答案（讲明白每一步）——拆解比解题可靠一个量级 |
| 无答案难题 | AI 说"这题我解不了" + 引导对话（学生说思路 → 归因知识点） |
| 落地成本 | judgeOne 加 L8 分支 + 两条 prompt，**零新增基建** |

**砍掉的过度设计**：~~难题解法库/AI 自学习~~、~~单生双画像~~、~~专属数据资产架构~~、~~外部符号工具~~、~~群体训练/微调~~——全部违反轻量化，不规划。

---

## 四、相关文件

- 标尺（已补锚）：`doc/architecture/D难度标尺-L11分层.md`
- 决策 011 无答案题三层流程：`doc/decision-log.md`
- 真题来源：2026 新高考Ⅰ卷数学第 19 题（博客园 heyang78/p/15234223 转录取证）
- 本轮临时脚本（/tmp，已不保留）：diag-pipeline.js / pattern-recog.js / solve-by-pattern.js

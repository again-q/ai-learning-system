# 编码审查包 — 知识图谱前端落地（图谱 P0）

> 用途：新会话验收入口。读取本文档 + 对照文件清单审查本轮改动，验收通过后用户再上传 git。
> 生成：2026-08-10
> 关联：`ROADMAP.md` 图谱 P0 小节、`output/knowledge-graph-prototype.html`（设计原型）

---

## 一、本轮改动一句话

**知识图谱 P0 三步走全部落地**：① 361 节点导入（含 path 修复）→ ② 云函数链路（graphService 修复 + knowledgeAdmin 新建）→ ③ 前端页面 pages/knowledge-map（结构+演化双模式）。

## 二、审查对象清单（按重要性排序）

### 🔴 云函数（已部署 cloud1）

| 文件 | 改动 | 审查关注点 |
|------|------|-----------|
| `cloudfunctions/graphService/index.js` | ① getGraph 根节点查询 `{level:1}` → `{parentId:null}` + `knowledgeId exists` 过滤 ② 新增 `getAll` action（全量拉取 limit 1000） | 旧 10 条数据（无 knowledgeId）不会命中；limit 1000 是否够（当前 371 条）；getAll 返回全字段含 concept/importance/relations 体积 ~200KB 是否可接受 |
| `cloudfunctions/knowledgeAdmin/index.js`（新建） | importNodes / importFromStorage / addNode / updateNode / deleteNode / stats 六 action | **无鉴权**（与 manageKnowledge 一致，管理操作任意 OPENID 可调——已知风险，是否需加管理员校验）；upsert 按 knowledgeId 查重逻辑；`db.command.size(0)` 统计 path 空 |

### 🟡 脚本

| 文件 | 改动 | 审查关注点 |
|------|------|-----------|
| `scripts/manage-knowledge-nodes.py`（新建） | 内置人教A版必修一章节映射，修复 270 个空 path，输出导入 JSON（check/gen 子命令） | 章节名映射表准确性（尤其 2.2 基本不等式、3.3 幂函数、5.6 等）；knowledge_id 解析（ch/s 段）健壮性 |
| `scripts/import-knowledge-graph.js` | 补 `relations`/`grade` 映射；排除 `knowledge_index.json`（防重复） | 与 manage-knowledge-nodes.py 功能重叠，二选一还是共存 |

### 🔴 前端页面（未上传，需开发者工具编译）

| 文件 | 改动 | 审查关注点 |
|------|------|-----------|
| `miniprogram/pages/knowledge-map/knowledge-map.js`（新建） | 双模式（结构=parentId 树 / 演化=reference 链）、Canvas 2D 画边、掌握度 mock、导航栈（_stack/_evoStack）、面包屑/回起点 | **Canvas 2D 初始化时序**（initCanvas 在 drawEdges 前调用，异步 query）；`nodesCount` 模块级变量在 render 重置；导航栈裁剪逻辑（onCrumbTap popCount 计算）；演化模式前置节点点击（_byId 查找） |
| `miniprogram/pages/knowledge-map/knowledge-map.wxml`（新建） | 节点层 wx:for + 角标 + 信息卡 | WXML 表达式（`item.mastery != null` 已改为 hasMastery 字段）；`style` 内字符串拼接 |
| `miniprogram/pages/knowledge-map/knowledge-map.wxss`（新建） | 苹果风、nodeIn 动画、ring 渐变环 | **mask 属性兼容性**（`-webkit-mask: radial-gradient` 在 skyline/webview 渲染差异） |
| `miniprogram/app.json` | 注册 `pages/knowledge-map/knowledge-map` | 页面顺序、tabBar 未动 |
| `miniprogram/pages/index/index.wxml` + `index.js` | 首页加「🗺️ 知识图谱」入口 + goGraph | 与现有布局兼容 |

### ⚪ 文档 / 参考

| 文件 | 说明 |
|------|------|
| `ROADMAP.md` | 图谱 P0 三步走完成落档；愿景/日期/决策记录 |
| `output/knowledge-graph-prototype.html` | 设计原型（HTML 版），本轮前端页面的视觉规格来源，**非生产代码** |
| `doc/review/启动包-拍照诊断最小闭环.md` | 项目入口文档（上一轮产物） |

## 三、部署与数据状态（已做，验收时复核）

- 云函数：`graphService`（含 getAll）、`knowledgeAdmin` 已部署 cloud1（`cloud1-d8g0ty39wd73f430a`），超时 60s
- 数据：`knowledge_nodes` 361 条新 schema 节点（knowledgeId/name/type/subject/stage/grade/level/parentId/path/concept/importance/relations），path 全部 4 级完整；另有 10 条旧版遗留数据（manageKnowledge 在用，未动）
- 验证过的调用：`getGraph`（根节点返回正常）、`getAll`（全量）、`knowledgeAdmin stats`（total 371, noPath 0）、`importFromStorage`（updated 361）

## 四、已知遗留 / 风险（审查时可确认是否接受）

1. **掌握度是 mock**（前端内置 MOCK_MASTERY + name hash 伪随机）——待 judgeOne 补知识点关联并写 `knowledge_progress` 后替换
2. **knowledgeAdmin 无鉴权**——任何 OPENID 可调 import/delete，管理风险（本地/内测期可接受？）
3. **relations.related 数据未消费**（决策：忽略，待定标准重生成）
4. **旧数据双轨**：knowledge_nodes 里 10 条旧节点 + manageKnowledge 旧集合仍被 knowledge-tree 使用，未清理
5. 诊断链路 5 云函数（photoUpload/diagnose/judgeOne/dispute）**云端未部署**（本地代码在）
6. `pages/graph`（五维雷达 mock）和 `pages/knowledge-tree`（旧树）未改造，与新页面并存

## 五、验收方法（新上下文）

1. 读本文档 → `git status` / `git diff` 核对改动范围
2. 重点走查：graphService 根查询、knowledge-map.js 导航栈与 Canvas 时序、knowledgeAdmin 幂等 upsert
3. 可选：开发者工具编译预览（首页 → 知识图谱）
4. 验收结论写入更新日志后用户上传 git

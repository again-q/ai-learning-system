# 掌握度链路 · 待解决问题清单

> 2026-08-14 重建后整理。优先级：🔴 高（影响数据准确性/主线功能）🟡 中 🟢 低
> 关联：`doc/ENGINEERING_TODO.md`（旧版总清单）、`doc/STATUS-MANIFEST.md`（状态快照）

---

## 一、判定准确性（🔴 影响数值可信度）

- [x] ~~**1. 同题判定漂移**~~ → ✅ **已解决（2026-08-14）**：删除未定义过的 isCorrect（伪参数），对错语义由 P 连续值承担——无二值可翻，4 次实测 P=0.90/0.95/0.90/0.90 稳定。D/η 也稳（D 0.55、η 0.70）。
- [x] ~~**2. 历史回填与新规则不一致**~~ → ✅ **已处理（2026-08-14）**：全部数据为测试数据（knowledge_progress 5 条 + node_progress 8 条已标 `isTest: true`），无需重算对齐。**规则（用户拍板）：掌握度错了直接删 knowledge_progress 对应记录，不做迁移/重算**。
- [ ] **3. 知识点匹配重构（待 #9 图谱补全后实施）**：「集合的包含关系」→ 归到「集合」等偏宽匹配。
  - **方案（用户拍板 2026-08-14）**：① 等知识点列表完整（#9）后，匹配逻辑封装成**独立函数**（搜索列表：精确 → 包含 → 相似度，稳定可测）；② 确认明显找不到时，**AI 提炼一个更详细的新知识点**自动补入。
  - **⚠️ 官方图谱保护（用户拍板 2026-08-14）**：上线后官方知识图谱（knowledge_nodes）**不可被系统/AI 自动修改**，只能官方（用户）改；AI 提炼的自定义节点**放在独立区域**（如 `custom_nodes` 集合，与官方隔离），图谱展示时合并但标记来源。
  - 关联：judgeOne 只做初判，二次复核页可人工改节点（根治闭环）。

## 二、功能缺口

- [ ] **4. S 维度未实现（🟡 中优先级，非 P0）**：失常信号（P 高但答错，如 P 0.8~0.95 且归因执行类）无处归档。公式现成：S0 = |P_exp − R_actual|。
  - 最小版：judgeOne 判题后把 {P, D, η, 时间} 追加记录，供 S0 计算（isCorrect/errorDimension 已删除 2026-08-14）。
- [x] ~~**5. mastery_logs 仍是空集合**~~ → ✅ **已解决（2026-08-14）**：judgeOne 每次 K 更新追加一条（userId/knowledgeNodeId/triggerQuestionId/oldMastery/newMastery/algorithm=weighted_score_v1/createdAt）。
- [ ] **6. T/Q 维度未实现（🟡 中优先级，非 P0）**：理论已定稿（ENGINEERING_TODO §二），工程未做。
- [ ] **7. A 维度无 UI（2026-08-14 A 已按宪法改单元级）**：A/U/N 已移至独立集合 unit_progress（宪法 §5.3 单元级）；图谱/页面仍只显示 K（mastery），A 展示未做。
- [x] ~~**8. 二次复核改知识点名不联动掌握度**~~ → ✅ **已解决（2026-08-14）**：updateParams 改名时自动迁移掌握度——
  旧节点 S_k/D_k 扣回（Dsum 归零删记录）、新节点添加（migrateProgress 函数，try/catch 不阻断保存）。

## 三、内容补全（🟡 用户指示「下次补」）

- [ ] **9. 图谱高频知识点缺失**：knowledgeUsage 剥离后大量匹配失败——实测 4 个知识点 3 个无节点：
  韦达定理、判别式、一元二次方程的解法（图谱按教材章节组织，缺通用术语节点）。
  - 用 `scripts/add-knowledge-node.py` 批量补，补完匹配率大幅提升。

## 四、数据卫生（🟡 低成本消噪音）

- [x] ~~**10. node_progress 8 条孤儿数据**~~ → ✅ **已删除（2026-08-14）**（用户拍板：测试数据直接删）。
- [x] ~~**11. knowledge_progress 混入 1 条异结构数据**~~ → ✅ **已删除（2026-08-14）**（用户拍板）。
- [ ] **12. knowledge_nodes 新旧 schema 并存（已查清待决策）**：359 条新 + 10 条旧（早期粗粒度：一次函数模型/二次函数基础/配方法/因式分解/三角·对数·数列模型等，字段 chapter/deps/difficulty）。
  旧 10 条只被 manageKnowledge 后台页（model-cards/method-list/knowledge-admin）使用，graphService 已按 knowledgeId 过滤不受影响。
  选项：后台页迁新 schema / 删旧 10 条 / 先不管（等 #16 双轨收口时一并处理）。
- [x] ~~**13. judgeOne 里 isRecallQuestion 已冗余**~~ → ✅ **已处理（2026-08-14）**：保留输出，代码已加注释说明（未来报告/统计可用，当前 K 整题更新不区分）。

## 五、决策落地（🔴 防遗忘）

- [x] ~~**14. 本次重建决策记入 decision-log / ROADMAP**~~ → ✅ **已落文档（2026-08-14）**：决策 025（P 连续/isCorrect 删除/fiveDim 0~1/K 整题/A 方向/匹配函数化/mastery_logs 补写/迁移），宪法与官方图谱保护也已记入项目记忆。

## 六、协作（🟡）

- [ ] **15. 本地 review 链路提交未 push**：远端分叉持续扩大风险，需用户确认后 push。

## 七、既有技术债（🟢 非本次引入，STATUS-MANIFEST 记录）

- [ ] 16. manageKnowledge 与 knowledgeAdmin 双轨
- [ ] 17. dimension-detail / model-cards / method-list 占位无入口
- [ ] 18. 首页（index）数据是否仍 mock，需核对

---

## 建议处理顺序

```
第一批（决策落地+消噪音）：14 → 10 → 11 → 12
第二批（数值可信度，P0）：1 → 2 → 3
第三批（链路补全，P0）：5 → 8
第四批（内容）：9（下次补）
第五批（中优先级，非 P0）：4（S）→ 7（A 展示）→ 6（T/Q）→ 16 → 17 → 18
```

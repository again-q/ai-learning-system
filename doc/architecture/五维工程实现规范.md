# 五维工程实现规范

> 生成：2026-08-17
> 定位：从五维理论文档（宪法，`doc/theory/五维能力向量框架-理论文档.md`）**拆出的工程化实现细节**。宪法只保留理论（定义/公式/参数语义/评分标准），本文件收纳执行层内容（LLM 判定、prompt、存储字段、交互采集、检索、展示文案）。每节标注原章节出处，保持可追溯。
> 拆分原则（2026-08-17 用户拍板）：理论 = 公式/参数/评分标准/维度关系；工程 = 执行者（LLM 判）、交互、存储（字段/集合）、检索（RAG）、展示（文案）。
> 交互形态（2026-09 决策 030 更新）：Q/S 主观信号采集 = 报告页视觉引导回顾（系统判值触发，见 §五/§六），非做题中弹窗打断。

---

## 〇、判定执行者总则（原 §4.4/§5.5/§6.4）

- D、P、η、r、T_shown、W 等参数**全部由 LLM 判定**，具体模型/参数/标尺见 `doc/architecture/诊断引擎-六阶段流水线.md` 与 `诊断输出规范v1.md`。
- 判定 prompt 的约束与锚点见云函数 `cloudfunctions/judgeOne/index.js`（RUBRIC/RUBRIC_V2）。

---

## 一、数据流工程层（原 §2 Pipeline 的工程动作）

```
拍照 → Qwen 图像转录（题目+做题痕迹）→ AI 拆题
  → judgeOne 逐题判定（D/P/η/归因/knowledgeUsage/pattern）
  → 掌握度更新（K 知识点粒度 / A 单元级）
  → 写入 questions / knowledge_progress / unit_progress / mastery_logs
  → text-embedding-v4 向量化 → RAG 自增强
  → 报告生成（Function Calling 调 ragService）
```

---

## 二、K 维度工程实现（原 §4.4 修订块、§4.2 修订块）

### 2.1 知识粒度拆解（决策 026）

判定时输出 `knowledgeUsage`：

```js
knowledgeUsage: [
  { name: "知识点教材术语", P: 0|0.5|1, D: 0~1 }   // D_kp：该知识点环节在本题的难度
]
```

- **P_kp 三档判定锚点**（由判定 prompt 按知识点自身约束，不在宪法定死）：
  - 1 = 用对
  - 0.5 = 用了但漏边界/不完整（漏特殊值验证、多选漏特殊情况、知识边界掌握不清）
  - 0 = 该环节缺失或全错
  - 涉及边界/陷阱/分类讨论的环节漏了特殊情形 → 0.5；环节根本没做 → 0
- 更新：`S_k += D_kp × P_kp`、`D_k += D_kp`，每知识点 upsert `knowledge_progress`

### 2.2 存储

- `knowledge_progress`：sValue / dValue / mastery / attempts / correctCount / lastUpdated（correctCount 只计 P_kp=1 的次数）
- `mastery_logs`：每次 K 变更 append（oldMastery/newMastery/algorithm=weighted_score_v1）——趋势/重算依赖

### 2.3 模型层 → RAG（D-18/D-19）

- 判定时生成题型三层结构 `pattern: {domain, pattern, variant}`（domain 知识板块 / pattern 中粒度题型 / variant 变体）
- 写入 `mastery_logs.pattern` + `patternEmbedding`（题型文本向量）
- 检索：`ragService.vectorSearch`——query = AI 生成的题型描述 → patternEmbedding 近邻（阈值 0.70）→ 同题型内按整题 embedding 排序

---

## 三、A 维度工程实现（原 §5.5）

- η（路径质量系数）：LLM 分析学生步骤文本判定，0.4~1.0，仅解答题
- 存储：`unit_progress`（aValue / aUpper / n / lowEtaStreak）
- 归因分流（决策 026 随 USE_KNOWLEDGE_USAGE 启用）：判错且 errorDimension=K/S → E=0 不进 A

---

## 四、T 维度工程实现（原 §6.4）—— ⏳ 暂缓

- W（跨类型权重）：LLM 按知识图谱判断
- r（解题质量系数）：需「学生说出深层结构」——依赖多轮引导/对话能力，拍照单向产品拿不到 → **暂缓**（后置 AI 教练对话模块）
- T_shown：LLM 综合作答+思维链判断
- 存储：每学生一个 T/TU（初始 0.10/0.60）

---

## 五、Q 维度工程实现（原 §7.4~7.6）—— 交互形态已定，实现随判定域图化推进

> 2026-09 决策（弹窗 → 报告页视觉引导回顾）：Q 只在系统判值为高价值题的回顾位上采集，不弹窗、不打断做题。

- 触发（判值器，代码规则，无 LLM）：判定产物进判值器 → 高价值题（做错 / 做对但过程复杂 / 关键节点题）→ 该题在报告页带 **Q 型回顾位**（视觉引导思维链采集）
- 引导强度随 Q 自适应（保留宪法「采集频率与 Q 负相关」）：Q 低 → 回顾位前置高亮、强引导；Q 高 → 折叠、可跳过、不催促
- **防锚定（Q2/Q4 测量前提）**：回顾采集必须先于该题 AI 断点/归因展示——高质量题**整题锁**（对错/断点/归因/参考过程均不开放），学生先诉说「做题感受与思路」后解锁整份解析 + 对照反馈（决策 031，v1.5；连对错也不先行展示——对错会锚定诉说、污染 Q1 事中监控）
- 评分：LLM 按 Q1~Q5 标准给 0~1 分；移动平均更新（冷启动 N<3 放大步长）；Q5 首次占位 0.5、下次同类题验证后更新（不变）
- 存储：全局 Q 指数 + Q1~Q5 移动平均 + N（不变）

---

## 六、S 维度工程实现（原 §8.3 数据来源、§8.7 输出）—— ⏳ S0 可先做，S1~S4 依赖回顾位设计

- S0 波动指数（客观）：`P_exp = w_K×K + w_A×A + w_T×T`（单元内 w=0.3/0.7/0；跨单元 0.2/0.4/0.4）；`d = |P_exp − R_actual|`；`V = 最近 N 次 d 平均`（N=20~30）——依赖 K/A/T 有值，可独立实现（每次判定后算一次 d，滚动窗口）
- S1~S4（主观，2026-09 决策：不再依赖 Q 弹窗）：S0 判失常的题（d 大，P_exp 已排除能力题）带 **S 型回顾位**；与 Q 同题触发时，先 Q 自由叙述、后 S 归因（让学生指过程位置，不先给归因标签选项——防锚定）；学生自报**逐字采纳**，AI 过程比对降级为报告层对照反馈（冲突本身作为报告内容，不作采集校验）
- S5/S6（归因，不计入 S_global）：轻量自评 / 考试上传询问（非报告场景，保留弹窗形态可选）
- 输出文案示例（原 §8.7）：稳定性指数 + 波动指数 + 失常画像 + 状态/心态记录——前端展示层设计

---

## 七、其他工程决策索引

| 项 | 位置 |
|---|---|
| 判定 prompt / 标尺 | `cloudfunctions/judgeOne/index.js` |
| D 难度标尺 L1~L11 | `doc/architecture/D难度标尺-L11分层.md` |
| 六阶段流水线 | `doc/architecture/诊断引擎-六阶段流水线.md` |
| 诊断输出规范 | `doc/architecture/诊断引擎-诊断输出规范v1.md` |
| RAG 工具契约 | `doc/architecture/RAG工具契约.md` |
| 决策记录 | `doc/decision-log.md`（025/026 等） |

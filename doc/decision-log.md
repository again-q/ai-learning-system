# 决策记录

> 本文件记录项目所有关键决策的背景、方案对比、选型理由和最终结果。

## 维护原则（2026-08-07 起强制）

**本文件只追加，不删除、不原地改史。**

| 允许 | 禁止 |
|------|------|
| 追加新决策条目 | 删除已有决策正文 |
| 在旧条目**下方**追加「纠偏 / 后续 / 废止」段 | 为「写对」而覆盖改写旧字段原文 |
| 状态改为 `❌ 已废止` 并指向替代决策号 | 假装从未选过错方案 / 从未写错过 |

- 踩坑、否决过的方案、换工具过程必须留下（见决策 015 体例）。
- 不必收录所有闲聊；必须收录：候选、否决理由、证据、最终契约、未决项。
- **缘起**：2026-08-07 纠偏五维表述时，曾直接改写决策 011/014 表内原文，与「可回溯」目标冲突；已按本原则恢复原文并将纠偏改为追加（见各条下「纠偏」段与决策 016）。

---

## 决策 001：PDF 解析方案选型

| 字段 | 内容 |
|------|------|
| **时间** | 2026-07-28 |
| **状态** | ✅ 已定案 |
| **背景** | 教材 PDF 经 PyMuPDF 检测为图片型（267 页，0 文字，268 张图片），无法用 pdf.js 直抽 |
| **方案** | 腾讯云 OCR `GeneralAccurateOCR`（通用文字识别高精度版）|
| **理由** | MinerU 在 Mac 上因 MPS 兼容性+网络+速度问题无法使用；ppstructure 安装复杂；腾讯云每月 1000 次免费额度，整本 267 页在额度内 |
| **验证** | 封面页 98%、公式页 96%、文字页 94-98% 置信度，1.3 节 9 页全通过 |
| **产出路径** | `output/raw/ocr_pageXXX.json`（逐页）→ `output/sections/{section_id}.txt`（每节合并）|

---

## 决策 002：三 Agent 管线架构

| 字段 | 内容 |
|------|------|
| **时间** | 2026-07-28 |
| **状态** | ✅ 已定案 |
| **背景** | 知识点提取需要分步处理：先整理排版，再提取知识点，最后质检。单一 Agent 做不到高质量 |
| **方案** | Agent 1（版面分析）→ Agent 2（知识点提取）→ Agent 3（质检），Shell 脚本串行调度 |
| **调度方式** | `qwenpaw agent chat` CLI，不传 `--session-id` 每节新开会话 |
| **失败处理** | 重试一次，仍失败记入 `_failed.log`，跳过该节不阻塞后续 |

---

## 决策 003：知识点不设数量限制

| 字段 | 内容 |
|------|------|
| **时间** | 2026-07-28 |
| **状态** | ✅ 已定案 |
| **背景** | Agent 2 Prompt 最初要求每节 8-15 个节点，但实际内容决定节点数，强行上下限不合理 |
| **决定** | 取消上下限，Agent 2 自由判断该提取几个节点 |
| **1.1 结果** | 11 个节点 |
| **1.2 结果** | 10 个节点 |
| **1.3 结果** | 10 个节点 |

---

## 决策 004：质检不暂停，记日志

| 字段 | 内容 |
|------|------|
| **时间** | 2026-07-28 |
| **状态** | ✅ 已定案 |
| **背景** | 质检不通过时如果暂停等人工确认，整本 24 节跑不完 |
| **决定** | 质检不通过→写入 `needs_review.log`→自动继续下一节。全部跑完后人工批量处理 |

---

## 决策 005：1.3 测试结果记录

| 字段 | 内容 |
|------|------|
| **时间** | 2026-07-28 22:44 |
| **耗时** | Agent 1: 138s | Agent 2: 73s | Agent 3: 127s | 总: 338s |
| **结果** | ✅ 通过，0 阻塞，2 警告（#12 补集遗漏→OCR截断非Agent问题，#13a 并集↔交集不合并→合理）|
| **产出** | 10 个节点，累计 31 节点（1.1=11 + 1.2=10 + 1.3=10）|

## 决策 006：整本管线跑通（21/24节完成，318节点）

| 字段 | 内容 |
|------|------|
| **时间** | 2026-07-28 |
| **状态** | ✅ 完成 |
| **完成度** | 21/24 节通过质检，318 节点 |
| **通过节** | 1.1-1.5（第一章全部）、2.1-2.3（第二章全部）、3.1-3.4（第三章全部）、4.1/4.5、5.1-5.7（第五章全部）|
| **待跑节** | 4.2 指数函数 / 4.3 对数 / 4.4 对数函数（Agent 1 返回空）|
| **需复核** | 13 节标记 `needs_review`，多为 pipeline 默认标记，大部分实际通过质检 |
| **脚本** | `scripts/pipeline-all.sh` 全自动三Agent + `scripts/retry-4.2-4.4.sh` 重跑 |

## 决策 007：TOC 页码偏移教训

| 字段 | 内容 |
|------|------|
| **时间** | 2026-07-28 |
| **问题** | TOC 记录的是教材印刷页码，但 PDF 物理页码偏移了 +6。直接按印刷页码跑 OCR，产出了 155 页全错的内容 |
| **根因** | 没有验证 OCR 第一页的实际内容是否匹配章节标题 |
| **预防** | ① OCR 前必须用 PyMuPDF 或页脚 OCR 验证 TOC 页码与 PDF 物理页的偏移量 ② 每个章节的第一页 OCR 后必须检查内容是否包含该章节标题 ③ 下次跑其他教材时先跑 1 个章节验证，不要整本盲跑 |

## 决策 008：文件重组与知识图谱独立文件夹

| 字段 | 内容 |
|------|------|
| **时间** | 2026-07-28 |
| **问题** | `output/` 目录混放 OCR 原始JSON、Agent 中间产物（版面 .md）、最终节点 JSON、质检报告，定位困难 |
| **方案** | 最终产出（节点JSON + 索引）移至 `knowledge-graph/`，中间产物留在 `output/` |
| **结果** | `knowledge-graph/nodes/` 21 个独立节文件 + `knowledge_index.json` |
| **后续** | 4.2-4.4 跑通后手动复制到 `knowledge-graph/` 即可 |

## 🗺️ 知识图谱形成全过程回顾

```
需求：教材PDF → 结构化知识图谱
  │
  ├─ [7月28日 15:54] 解析方案调研
  │    MinerU (否掉) → PaddleOCR (否掉) → 腾讯云OCR (选中)
  │    原因：Mac MPS 兼容性 + 网络下载 + 速度问题
  │    → 决策001：腾讯云 GeneralAccurateOCR
  │
  ├─ [7月28日 16:10] 架构设计
  │    单一Agent不够 → 三Agent管线
  │    → 决策002：Agent1(版面) → Agent2(提取) → Agent3(质检)
  │    → 决策003：不设节点数上限
  │    → 决策004：质检不暂停，记日志
  │
  ├─ [7月28日 16:46] OCR 整本
  │    267页，腾讯云API，244次调用（封面+目录+24节）
  │    踩坑：TOC页码偏移+6，重跑155页 → 决策007
  │    产出：output/sections/ 24个.txt
  │
  ├─ [7月28日 22:44] 1.3 测试
  │    A1=138s A2=73s A3=127s 总338s
  │    10节点，通过✅ 0阻塞
  │
  ├─ [7月28日 23:00~] 整本管线
  │    用 pipeline-all.sh 自动跑
  │    21/24节通过，318节点
  │    失败：4.2-4.4（Agent1 返回空）→ retry-4.2-4.4.sh
  │
  ├─ [7月28日] 后处理修复
  │    type规范化：concept→definition, theorem→property 等
  │    ID修复：__ → _
  │    跨节引用修复：math_10_ch2_s2 → math_10_ch2_s2_008
  │    冗余节点删除：2.1-010(章前言), 3.2-018(合入001)
  │    缺失节点补充：3.4 蓄水池造价问题
  │    索引补录：s4 补入 knowledge_index.json
  │
  └─ [7月28日 23:00] 文件重组
       知识图谱独立目录 → knowledge-graph/
```

### 各节节点数（最终）

| 章 | 节 | 节点数 |
|----|----|-------|
| 第一章 集合与常用逻辑用语 | 1.1 集合的概念 | 11 |
| | 1.2 集合间的基本关系 | 10 |
| | 1.3 集合的基本运算 | 10 |
| | 1.4 充分条件与必要条件 | 14 |
| | 1.5 全称量词与存在量词 | 13 |
| **第一章小计** | | **58** |
| 第二章 一元二次函数、方程和不等式 | 2.1 等式性质与不等式性质 | 14 |
| | 2.2 基本不等式 | 12 |
| | 2.3 二次函数与一元二次方程 | 11 |
| **第二章小计** | | **37** |
| 第三章 函数的概念与性质 | 3.1 函数的概念 | 15 |
| | 3.2 函数的基本性质 | 18 |
| | 3.3 幂函数 | 15 |
| | 3.4 函数的应用（一） | 14 |
| **第三章小计** | | **62** |
| 第四章 指数函数与对数函数 | 4.1 指数 | 14 |
| | 4.5 函数的应用（二） | 22 |
| | *(4.2-4.4 待跑)* | |
| 第五章 三角函数 | 5.1 任意角和弧度制 | 13 |
| | 5.2 三角函数的概念 | 14 |
| | 5.3 诱导公式 | 13 |
| | 5.4 三角函数的图象与性质 | 29 |
| | 5.5 三角恒等变换 | 30 |
| | 5.6 函数 y=Asin(ωx+φ) | 14 |
| | 5.7 三角函数的应用 | 12 |
| **第五章小计** | | **125** |
| **全书合计** | **21/24节** | **318** |

---

## 决策 009：全书交付（24/24节，359节点）

| 字段 | 内容 |
|------|------|
| **时间** | 2026-07-29 |
| **状态** | ✅ 完成，已推送 GitHub |
| **最终产出** | `knowledge-graph/` — 24 个独立 JSON + 总索引，359 知识点节点 |
| **通过率** | 24/24 节通过 Agent 3 质检 |
| **后续修复** | 补集缺失（OCR追加第19页→重跑A1→A2→A3→入库）|

## 决策 010：知识图谱构建经验总结

### 🔴 TOC 页码偏移（最贵的一课）

**问题**：TOC 记录教材印刷页码，PDF 物理页码偏移 +6，直接按印刷页码跑 OCR 导致 155 页全错，重跑浪费数小时。

**预防**：OCR 前必须用 PyMuPDF 验证 TOC 页码与 PDF 物理页的偏移量，每章第一页检查内容是否含章节标题。

### 🔴 AI 生成的 JSON 内引号问题

**问题**：Agent 2 输出的 JSON 字符串内部包含 ASCII `"`（如 `称为"补集"`），破坏 JSON 结构。

**根因**：大模型在中文上下文中使用 ASCII 双引号作为中文引号，而 JSON 要求内层引号转义或使用 `「」`。

**修复**：Python 正则 + 逐行字符扫描替换内层 `"` 为 `「」`。

**预防**：Agent 2 Prompt 应明确要求「JSON 字符串内部禁止使用未转义的 ASCII 双引号，中文引用请用「」」。

### 🟡 文件命名不一致

**问题**：OCR 文件命名 `math_10_ch1_4` vs Agent 1 产出 `math_10_ch1_4_充分条件...` vs Agent 2 期望 `math_10_ch1_s4`，导致跳过逻辑失败。

**教训**：section_id 必须从输入到输出全程一致，脚本中用 `cut -d'_' -f1-4` 提取纯 ID。

### 🟡 Schema 漂移

**问题**：有的节用旧 schema（`knowledge_id/basic/tree/concept/relations `），有的用新格式（`id/title/content/key_terms/references `），质检 Agent 按旧格式校验新格式时标记为质量差。

**教训**：Agent Prompt 必须锁定 JSON schema 版本，不允许模型自行决定输出格式。

### 🟢 全自动管线验证成功

**经验**：Shell 脚本 + QwenPaw Agent CLI 的三阶段管线（A1 版面→A2 提取→A3 质检）跑通 24 节，单节失败不阻塞后续。

### 🟢 文件归档策略

**经验**：最终产出（节点 JSON）与中间产物（OCR 原始 JSON、Agent 版面 .md）分离存放，`knowledge-graph/` 独立目录免检索干扰。

### 🟢 补集修复流程

**经验**：单节点内容缺失时，不需要重跑全书。追加 OCR → 单节重跑 A1→A2→A3 → 更新索引，15 分钟内解决。

---

## 决策 011：无标准答案题的三层处理流程

| 字段 | 内容 |
|------|------|
| **时间** | 2026-07-30 |
| **状态** | ⏳ 待确认（3 个待定点未拍板） |
| **背景** | 拍照录入的题目可能没有标准答案（主观题/无确定解/图形残缺）。此前设计的「对错判定」依赖标准答案，无答案时无法判定 |
| **方案** | 三层流程：① AI 判可解性（不看学生答案）→ 能则 AI 算答案走正常 K/A/T 全过程；不能则 ② 过程诊断（η 路径质量 + r 解题质量 → 更新 A/T，K 冻结）+ 思维链弹窗重点采集 Q；③ S 维度轻量自评弹窗「这题你觉得能拿几分？」→ 比对 AI 过程分析 → 生成稳定性报告 |
| **理论依据** | K 冻结：无答案判不了掌握与否，对错是 BKT 更新掌握概率的观测输入；A/T 靠过程质量：过程比结果更能反映应用/迁移能力；S 自评偏差：自评与实际表现偏差 = 稳定性信号；思维链弹窗只在第二步触发 = 符合《21项学习技术》「信号题才深挖」原则 |
| **待确认点** | ① η/r 定义与取值区间（建议 0-1，Flash 计算，存 mastery_logs）；② AI 可解性判断是否需要置信度阈值（建议 ≥0.8）；③ AI 算的答案标记 `answerSource:'ai_computed'`，低置信度时不更新 K 只更新 A/T（防 AI 错答案污染 K） |
| **参考** | 《理科学神训练体系-21项学习技术》21.2 错误根源诊断、19 解题过程回溯 |

#### 纠偏（2026-08-07）— 追加，不改上表原文

对照 `doc/theory/五维能力向量框架-理论文档.md` 后的认识（上表仍保留当日原文，便于看出曾误写 BKT）：

1. **K 不是 BKT**：理论为加权得分法 `S_k/D_k`；「对错是 BKT 观测输入」表述作废，改以过程分 P / 合格观测解释无答案路径为何草案冻结 K。
2. **η 区间**：理论 A 维 η ∈ **0.4~1.0**（上表「建议 0-1」过宽）；r（T）仍 0~1。
3. **「能则走正常 K/A/T 全过程」过粗**：维度路由与 **K 更新时机**未拍板（理论「仅回忆类」vs 旧引擎「有答案即可」），见流水线 M4 /《诊断引擎设计》待讨论；A/T 仍按单元内 / 跨单元。
4. **过程证据**：无答案第二步所依赖的过程，须经决策 015 校准后的 `process_evidence`，不能单靠未校准视觉过程。

## 决策 012：归因模块 MVP 深度

| 字段 | 内容 |
|------|------|
| **时间** | 2026-07-30 |
| **状态** | ⏳ 待确认（选项 A/B 未拍板） |
| **背景** | 报告五层结构（见 `诊断报告设计.md`）第⑤层归因。借鉴《21项学习技术》：归因不是贴标签（粗心/不会），是追根源（直接原因 → 思想根源） |
| **方案A（推荐）** | 每题：证据（Qwen 痕迹 + 学生口述）→ 直接原因（概念缺失/方法选择/计算执行/审题条件/过程回溯）→ 行动建议；「思想根源深挖」只对信号题触发（反复错/明明会却错/变形就崩），用书里的「信号清单」 |
| **方案B** | 完整四步法全做：每题都走到思想根源七层（知识/经验/格局/技术/策略/情绪/资源）→ 行动。准确但复杂、慢、费 token，MVP 周期长 |
| **要点** | 「粗心」「马虎」不是合格的直接原因，只是新现象，要继续研究到可行动；归因与行动建议是一条链：根源决定行动 |

## 决策 013：对错判定逻辑（标准答案的有无决定判定方式）

| 字段 | 内容 |
|------|------|
| **时间** | 2026-07-30 |
| **状态** | ✅ 已定案 |
| **背景** | 用户指出「标准答案」是外部输入，此前数据来源清单漏了它。对错判定不让学生做，由系统判定 |
| **方案** | 有标准答案（学生录入或 AI 识别）：Flash 对比标准答案 vs 学生作答 → 判定 `isCorrect`，置信度高；无标准答案：走决策 011 三层流程（AI 判可解性，能算则 AI 答案当标准答案，不能算则过程诊断） |
| **优先级** | 学生录入的标准答案 > AI 从图中识别（图中印的答案可能识别错） |

---

## 决策 014：五维评分计算方式（AI 填参数 + 硬代码算）

| 字段 | 内容 |
|------|------|
| **时间** | 2026-07-30 |
| **状态** | ✅ 已定案 |
| **背景** | 五维评分若由 AI 直接输出分数，不可复现、不可解释、不可校准、可能带幻觉 |
| **方案** | Flash 调 `scoreFiveDim` 工具：输入证据（对错/η/r/归因/自述/历史）→ 输出公式参数；云函数硬代码算分。确定性计算，同参数同结果 |
| **原则** | **有公式的用公式，没公式的搁置**——K/A/T 有理论公式（BKT、η×权重）走硬算；Q/S 无完整公式则搁置不硬凑（S 只采集自评偏差信号，Q 等理论细化） |
| **衔接** | η/r（决策 011）是公式参数非终值；mastery_logs 记 `algorithm:'score_v1'`；工具包新增 scoreFiveDim |
| **产出** | `doc/architecture/五维评分计算-设计决策.md` |

#### 纠偏（2026-08-07）— 追加，不改上表原文

上表「BKT、η×权重」「Q/S 无完整公式」为当日误写/易误解表述，保留原文；现行理解如下：

1. **K**：加权得分法 `S_k/D_k`（理论 §4.4），**非 BKT**。
2. **A/T**：按理论 A/U、T/U 原文（§5.5 / §6.5），不是「η×权重」口糊。
3. **η**：A 维 0.4~1.0；**r**：T 维 0~1。
4. **Q/S**：「搁置」指**诊断引擎 scoreFiveDim 本阶段未落**，理论 §7/§8 公式已有（见流水线 M6/M7）。
5. **K 何时更新**：未拍板（见 M4）；与本决策「怎么算」分开。

现行设计文档（`五维评分计算-设计决策.md` 等）已按上述理解更新；**决策表原文不回溯篡改**（原则见文首 + 决策 016）。

---

## 决策 015：过程证据采集 — 视觉逐步置信度 + 语音/文字 + 上传侧过程 AI

| 字段 | 内容 |
|------|------|
| **时间** | 2026-08-07 |
| **状态** | ✅ 已定案（方案定；ASR 选用与置信度阈值等工程细节可后续补） |
| **收口** | ROADMAP 待决策 #9（视觉「舍去」误读 / 过程不可全信视觉） |
| **一句话** | 视觉仍负责过程，但逐步带置信度；学生用自然语言（语音优先，可打字）描述情况；上传界面另设「过程 AI」按学生说法灵活改过程草稿；确认后的 `process_evidence` 再交给 diagnose |

### 怎么来的：踩坑 → 换工具（过程复盘，必须保留）

> 本节不是事后美化结论，而是记录：**原先押什么工具 → 哪里翻车 → 讨论过换什么 → 最后换到什么**。

#### 坑 1：高估视觉模型「读过程」的能力

| | 内容 |
|--|------|
| **原先工具假设** | 拍照 → Qwen 多模态一次吃透「题文 + 做题全过程痕迹」→ 直接供 Flash 做 η/r、过程归因 |
| **踩坑事实（POC）** | 题文转录尚可（含一页多题）；**涂改密集的过程转录不可全信**——「舍去」标记挂错对象、≥/≤ 符号歧义 |
| **代价** | 若继续让视觉过程当事实，A/T 参数与归因会被脏数据污染；对错路径虽已用门禁2隔离，**过程路径仍裸奔** |
| **已写入** | ROADMAP「POC 测试结论」+ 待决策 #9（当时方案待定） |

#### 坑 2：差点用错药——「换视觉工具」

| | 内容 |
|--|------|
| **当场选项** | 换更强 VLM / 换视觉厂商，继续「一张图看懂全过程」 |
| **为何没走这条** | ① 题文已经够用，瓶颈在过程层通病，不单是 Qwen；② 换底座 ROI 低、工期与成本涨；③ 过程重要性越高，越不该押在最不稳的模态 |
| **结论** | **不优先换主视觉模型**；视觉保留，但降级为「过程草稿生产器」，不是过程真相源 |

#### 坑 3：只靠文字自述 vs MVP 真要上线

| | 内容 |
|--|------|
| **轻量补丁** | 纯打字自述，零新工具 |
| **为何不够** | MVP 直接给学生用；过程是一等信号，录入负担过高压坚持率；用户明确 **MVP 要上语音** |
| **含义** | 语音不是体验锦上添花，是开用必备 → 必须部署 ASR（新工具），但不能把 ASR 原文当真相 |

#### 坑 4：语音也会不准（第二噪声源）

| | 内容 |
|--|------|
| **新风险** | ASR 误识别 + 口语含糊 → 「转写完直接当过程」会第二次踩坑 |
| **不能做的事** | 视觉不准就改押语音；语音不准再空转 |

#### 换工具：最终换到哪一套

```
【旧工具链 — 已证伪】
  拍照 ──▶ Qwen（题+过程当事实）──▶ Flash 诊断
              ↑ 过程不可信（踩坑）

【讨论过但未采用】
  换更强 VLM 继续「看懂全过程」  ← 否决为优先解

【新工具链 — 本决策】
  拍照 ──▶ Qwen（题文高信任 + 过程逐步草稿+confidence）
              │
              ▼
         ASR 语音 / 打字（学生自然语言说情况）  ← 新增部署：ASR
              │
              ▼
         过程 AI（上传界面，编辑器）          ← 新增工具：校准用 AI
           按学生说法增删改过程草稿
              │
              ▼
         学生确认 → process_evidence → Flash 诊断
```

| 动作 | 工具 | 角色变化 |
|------|------|----------|
| **保留** | Qwen 视觉 | 从「过程真相」→「带置信度的过程草稿」 |
| **不换** | 主视觉底座 | 明确否决「换 VLM 治过程」 |
| **新增** | ASR（微信插件或腾讯云一句话等） | 语音→文本输入法；失败落打字 |
| **新增** | 上传侧「过程 AI」 | 唯一允许灵活改过程稿的层；不算分、不写报告 |
| **下游不变** | diagnose / Flash / scoreFiveDim | 只吃确认后的 `process_evidence` |

#### 时间线（同日讨论压缩）

1. 指出：曾高估视觉，POC 已写入项目  
2. 问：换工具还是改契约？→ 建议先改契约、不优先换 VLM  
3. 强调过程重要 → 讨论过程采集（自述/问答）  
4. 语音要部署新工具 + MVP 必须上语音  
5. 承认 ASR 也不准 → 提出上传页过程 AI 用自然语言灵活更新过程  
6. 用户确认 → 写入本决策（含本踩坑复盘）

### 方案（契约）

```
Qwen：题文（高信任）+ 过程逐步草稿 trace_steps[]（每步 text/type/confidence）
        ↓
学生：语音（ASR→文本，可打字覆盖）自然语言描述/纠正/补充
        ↓
过程 AI（上传界面，编辑器角色）
  输入：trace_steps + 学生文本（可能含 ASR 噪声）
  动作：增/删/改/合并步骤；标来源 vision | student | merged
  护栏：不判对错、不打五维、不无根据编造未提及细节
        ↓
学生确认（短摘要「当前理解的过程」→ 对 / 再说一句）
        ↓
process_evidence → 交给 diagnose / Flash（η、r、过程归因只吃这份）
```

| 角色 | 负责 | 不负责 |
|------|------|--------|
| 视觉 Qwen | 题文 + 带置信度的过程草稿 | 单独定最终过程 / 对错 |
| ASR / 打字 | 降低输入负担，产出学生文本 | 原文当过程事实 |
| **过程 AI** | 按学生说法编辑过程草稿 | 诊断评分、五维、报告 |
| diagnose Flash | 门禁 / 归因 / 填参 / 报告 | 在上传页改过程稿 |

### MVP 约束

- 语音与打字同一字段入口；ASR 失败自动落打字，不堵主流程  
- 先做「整段自然语言改稿」；针对第 N 步追问可二期  
- 低置信 / 冲突步骤：优先靠学生说法更新；无确认则过程证据降级（避免脏 η/r），具体阈值工程阶段再定  
- **不换**主视觉模型作为本问题的解法；过程 AI 才是过程可信度主修复器  

### 待工程补全（不阻方案定案）

1. ASR：微信语音插件 vs 腾讯云一句话（云函数）选型  
2. 逐步 `confidence` 阈值与「未确认是否允许送入 A/T」  
3. 过程 AI 的 JSON schema 与云函数拆分（建议独立于 `diagnose`）  
4. 回写《诊断引擎设计》第二阶段 / 流水线第一～三阶段接口字段  

### 关联

- ROADMAP POC 结论与待决策 #9  
- `doc/data/数据来源清单.md`（思维链双通道 → 升级为「草稿+校准」）  
- 决策 011（无答案过程诊断）、012（归因证据）、014（η/r 为公式参数）  


---

## 决策 016：决策记录文件维护原则 — 只追加、不删改史

| 字段 | 内容 |
|------|------|
| **时间** | 2026-08-07 |
| **状态** | ✅ 已定案 |
| **背景** | 讨论「decision-log 要不要删除内容，还是记录讨论过程」。同时发现：纠偏五维表述时曾**直接改写**决策 011/014 表内原文，与可回溯目标冲突，用户指出后要求补救并记录原则 |
| **踩坑** | AI 口头主张「只追加」，却对 011/014 做了原地改史；原则与行为不一致 |
| **方案** | ① 本文件**禁止删除**已有决策正文；② **禁止**为写对而覆盖旧字段；③ 新认识一律用「纠偏 / 后续 / 废止」**追加**在条目下方；④ 废止条目改状态并指向新决策号；⑤ 须保留踩坑、否决方案、换工具过程（体例见 015） |
| **补救** | 已将 011/014 **恢复为 main 上当日原文**，纠偏内容改为表下追加段；文首增加「维护原则」摘要 |
| **记什么 / 不记什么** | 记：候选、否决理由、证据、最终契约、未决项、踩坑换工具。不记：无决策含义的闲聊全文 |
| **约束对象** | 所有写入本文件的人与 AI |


---

## 决策 017：决策 015 修正 — Qwen 输出契约回到「整体把握散文」

| 字段 | 内容 |
|------|------|
| **时间** | 2026-08-07 |
| **状态** | ✅ 已定案（修正决策 015 的 vision 输出契约部分；015 其余内容仍有效） |
| **修正对象** | 决策 015 中「Qwen 输出 `trace_steps[]` 逐步结构化（每步 text/type/confidence）」一条 |
| **新契约** | Qwen 视觉继续输出**整体把握的 Markdown 散文**（题文转录 + 做题痕迹描述，即 POC 已验证的现状），**不切 trace_steps、不标逐步 confidence** |
| **理由（用户原话）** | 「毕竟是整体把握」——Qwen 的强项是对图片做整体把握，切步骤结构化会①把不可信的过程固化成确定结构，②制造虚假精确（LLM 自信≠准），③降低过程 AI 用自然语言校准的灵活性 |
| **仍然有效（015 其余部分）** | 视觉过程不可全信 → 学生语音/文字自述（ASR 新部署）+ 上传侧过程 AI 校准 → 学生确认 → `process_evidence` → 才进 diagnose；不换主视觉模型 |
| **接口衔接** | 视觉产出散文 → 过程 AI 输入散文 + 学生自然语言 → 输出（仍可内部按需组织）`process_evidence`；confirm 摘要回显用学生自己的话 |
| **影响** | `scripts/poc/vision-test.js` 的 VISION_PROMPT 保持现状（散文），无需改版 |


---

## 决策 018：报告异议机制 — 报告回显证据 + 「我觉得不对」入口

| 字段 | 内容 |
|------|------|
| **时间** | 2026-08-07 |
| **状态** | ✅ 已定案 |
| **背景** | POC 实测（图二·4道题随堂演练）暴露 3 个问题：① **vision 结论转录错误会直接污染 isCorrect**——学生实际写「a≥4」被读成「a>4」，DeepSeek 误判"漏了 a=4"，比过程污染更严重（门禁2 只隔离了过程，隔离不了结论转录）② **学生肉眼确认也有期望偏差**——用户自己扫视转录时没发现 ≥→> 错误（人脑用"应该是什么"自动脑补）③ 选择题只记选项序号不记选项内容，下一轮 judge 无法判定对错 |
| **方案** | 报告页每题：**回显证据**（题目转录 + 学生作答原文 + isCorrect 判定依据）+ **「我觉得不对」异议入口** → 弹出该题转录 → 学生修正（如「我是 ≥ 不是 >」）→ 修正 `process_evidence` → **单题重诊断**（不重跑整批） |
| **为什么放报告页** | 纠错动机时点不同：上传确认时学生急着做题、注意力散（会漏符号级错误）；报告出结果时学生想知道"为什么判我错"，动机最强、会主动逐字核对——此时回显证据零成本达成校准 |
| **复杂度收敛** | 与上传确认共用同一通道「修正 process_evidence → 单题重诊断」，不新增第二套逻辑；上传时快速确认 + 报告时异议兜底，一条链闭环 |
| **联动** | 决策 015（过程校准链）、决策 017（整体把握散文）、待决策 #10（低置信→高置信校准） |
| **遗留** | 选择题选项内容必须转录进契约（vision prompt 增补）；符号级转录（≥/≤、舍去范围）需在确认/异议界面结构化回显 |


---

## 决策 019：参数漂移实测 + 统一校对组件（字段级修订）

| 字段 | 内容 |
|------|------|
| **时间** | 2026-08-07 |
| **状态** | ✅ 已定案（方案）；漂移根因与参数 schema 落档留待详细设计 |
| **背景** | 同一份修正后的 process_evidence 喂 DeepSeek 跑 3 次，参数漂移严重：题4 isCorrect 三连判 false/true/true；题1 D 值漂移 4 倍（0.2~0.95）；题1 题型分类回忆/单元内 摇摆；r 出现 null 与 0 两种语义；归因第 1 次出现数学性幻觉。另：P 档位名、categoryReason、eta.confidence、errorAttribution 等字段为测试临时造，设计文档仅有定义（D/η/r 在理论文档，题型/归因在流水线）无字段级 schema |
| **方案：统一校对组件** | 上传确认（vision 结束）与报告异议（报告页）**共用同一采集机制**：结构化校对面板（字段级回显，符号高亮，AI 判定只读）→ 统一修订数据结构 `{scene, target{questionIndex,field}, original, revised, source:'student_revision', note, createdAt}` → 统一修订管道：更新 process_evidence → 单题重诊断 → 刷新报告 |
| **方案：参数级置信度** | 模型自评 confidence 不可靠（实测高置信仍判错）→ 置信度 = 规则层 + 模型层：isCorrect/题型分类**双跑一致性**判定（一致=高，分歧=低+报告标待核实）；D 只给档位；η 无过程强制低置信；r 无过程=null 禁止 0；归因强制带 confidence + 引用证据 |
| **漂移根因（初步）** | ① 温度未固定/无 seed → 采样随机放大 ② D/η 无 rubric 锚点（0~1 自由发挥）③ 题型分类边界 M4 未拍板 → 语义漂移 ④ r 无 schema 约束 ⑤ 归因开放生成易幻觉 ⑥ 长输入关键标记（舍去/保留）被漏读 |
| **联动** | 决策 014（AI填参+硬算）、决策 018（报告异议）、待决策 #10（校准设计）、流水线 M4/M5 |
| **遗留** | 参数字段级 schema 定义（P 档位名、归因结构、置信度字段）→ 详细设计阶段落档；temperature/seed 工程参数 |


---

## 决策 020：rubric 验证成功 + D 难度标尺定稿（L1~L11 高考锚定）

| 字段 | 内容 |
|------|------|
| **时间** | 2026-08-07 |
| **状态** | ✅ 已定案（对照方案保留于决策 019，不删改） |
| **验证结果** | 把理论文档判定表细化成 rubric（D 三档锚点/P 四档/η 本质vs暴力/r 语义）+ 固定 temperature=0.2 + process_evidence 关键标记结构化（保留项/舍去项）→ 重跑 3 次：**isCorrect 三连全对**（题4 正确判定 a≥4）/ **D 零漂移**（题1 恒 0.3）/ r 统一 null / 归因幻觉消失。对比决策 019 无 rubric 漂移数据，验证"理论文档细化成 rubric 喂 AI"是治漂移正解 |
| **残留问题** | ① η 无过程仍可能给分（rubric 写了 null 但模型偶尔不听话）→ **代码强制**：无过程证据 η 置 null，不信模型 ② 题型分类边界需判例库扩充（M4） |
| **D 难度标尺定稿** | **D = L1~L11 绝对难度标尺**（用户划分，固定锚点 **D=0.60 ↔ 高考 90 分及格线**）。AI 只判 Lx 单档（不允许合并档），5 维度降级为档内锁定分值；代码用 `D=(原始加权分−1)/4` 计算并钳制回 Lx 区间。L9+（D≥0.90）对应强基/竞赛，按理论超纲规则处理。完整标尺见 `doc/architecture/D难度标尺-L11分层.md` |
| **判定流程（两段式）** | ① AI 用边界速查卡判 Lx 档（分类任务最稳）② AI 在 Lx 锁定的 5 维分值范围内打分 → 代码算 D 并校验区间。双保险：档位卡漂移、公式保连续 |
| **题型/归因联动** | L1-L3∩回忆→K 问题（回课本）；L4-L6∩单元内→A 问题（专项流程训练）；L7-L9∩跨单元→T 问题（高观点工具）；L10-L11→超纲不计错。诊断信号列直接作为归因映射（决策 012 衔接） |
| **联动** | 决策 014（AI填参+硬算）、决策 017（整体把握散文）、决策 018（报告异议）、决策 019（对照）、流水线 M4 |
| **遗留** | η 代码强制规则入详细设计；题型判例库扩充（M4 细化）；参数字典整体文档待详细设计阶段整合 |


---

## 决策 021：D 判定流程 v5 定稿 — 档位钳制 + 档内自由取值

| 字段 | 内容 |
|------|------|
| **时间** | 2026-08-07 |
| **状态** | ✅ 已定案（修正决策 020 的判定流程部分；L1~L11 标尺本身不变） |
| **演进过程（实测驱动）** | ① 决策 020：5 维打分加权算 D → 实测 D 漂移 4 倍（0.2~0.95）② 约束修正：AI 只出分类 + 代码算 D → 全压区间下沿（0.15/0.45/0.60）③ 加例子锚（few-shot 对照）→ 档位全稳但 D 固定中值无区分度 ④ **v5 定稿：档位钳制 + 档内自由取值** |
| **v5 判定流程** | 第 1 步：AI 判 Lx 档（分类任务 + 例子锚参照，边界锁死）；第 2 步：AI 在 Lx 区间内自由打连续 D 值（档内相对难度：比锚例简单靠下沿/相当中段/多变形靠上沿）；第 3 步：代码钳制 D ∈ [Lx_lo, Lx_hi]（越界钳回边界） |
| **实测结果（同一 4 题数据，3 次）** | 题1 L1(0.08×3)、题2 L3(0.38×3)、题3 L4(0.52/0.52/0.55)、题4 **L4(0.58×3)**——档位 12/12 一致，D 无钳制发生，η 规则稳定（题1/2 null、题3/4 0.9~0.95），isCorrect 全对，r 全 null，归因干净。题4 从早期 L5 虚高修正为 L4（中档偏下，用户判定） |
| **核心认知** | 档位=硬边界（分类，卡大漂移）；档内=轻回归（自由，保微区分）。**AI 擅长分类不擅长打连续值**——让 AI 在窄区间（宽度≤0.15）内自由发挥，漂移被边界兜住。5 维加权公式保留为「校验器」（人工标定时校验 D 落档），不再是主判据 |
| **RAG 排除** | D 难度不用 RAG 实时学习——难度是"绝对属性"（因题而定），RAG 检索"相似题平均难度"会引入表面相似但难度不同的噪声；RAG 只用于诊断证据检索（历史掌握度/同类错题），与难度标尺正交 |
| **联动** | 决策 014（AI填参+硬算）、决策 019（无 rubric 对照）、决策 020（L1~L11 标尺）、流水线 M4 |
| **遗留** | 例子锚库扩充（每个 L 档更多锚例）；档位钳制逻辑入详细设计代码 |


---

## 决策 022：Dify 定位评估 — 可视化原型工具，生产路径待实测

| 字段 | 内容 |
|------|------|
| **时间** | 2026-08-08 |
| **状态** | ✅ 定位已定（MVP 不引入）；阶段 B 双路径留待实测选优 |
| **背景** | 用户调研 Dify（开源 LLM 应用平台，v1.16，支持工作流/RAG/Agent/图片输入/DeepSeek+Qwen 接入）后提出：可在 Dify 搭完工作流后转成代码，或直接调用 Dify API，实测对比性能选优 |
| **Dify 能力核实** | 工作流 API（/v1/workflows/run）、图片上传（≤10MB）进工作流、Human Input 节点（对应学生确认环节）、DeepSeek/Qwen 官方支持；开源 DSL 文件可导入但**半可复现**（缺模型 key/知识库/版本兼容） |
| **定位（阶段 A）** | Dify 用作**可视化原型工具**——快速编排节点、调 prompt、验证逻辑；适合未来 AI 教练（对话 Agent + RAG）快速原型 |
| **定位（阶段 B）** | 两条落地路径待实测选优：① **转成代码**（本 POC 已验证可行——rubric/判定/钳制全部可代码化，决策 015~021 管线即代码形态）② **直接调 Dify API**（云函数转发，工作流跑在 Dify）。对比维度：延迟（直调最短 vs Dify 多一跳）、成本（纯按量 vs 服务器/订阅）、确定性（决策 014 要求硬代码算，代码直写最优）、可观测性（Dify 内置 LLMOps vs 自建日志） |
| **当前 MVP 结论** | **继续自建代码管线**（决策 015~021 已定稿跑通）；Dify 不引入生产；评估结论防止重复调研 |
| **联动** | 决策 014（确定性计算原则）、决策 015~021（自建管线）、AI 教练子项目（未来 Dify 原型候选） |
| **遗留** | 阶段 B 实测（转代码 vs 调 Dify）在 AI 教练或生产化阶段执行；Dify 模板生态无教育诊断场景，无可复用资产 |


---

## 决策 023：难题诊断 = 诚实引导者 + 双轨学习（不依赖模型解题）

| 字段 | 内容 |
|------|------|
| **时间** | 2026-08-10 |
| **状态** | ✅ 已拍板（方向定稿，落地待排期） |
| **背景** | 2026 新高考Ⅰ卷 19 题（17 分压轴）实测：模型解不出难题（109s 卡住）、难度判定严重低估（0.76 vs 真题 0.92）、模式识别骨架可信但细节错。结论：诊断不能依赖模型解题能力、标准答案、学生自述卡点（三者均不可靠） |
| **方案** | AI 角色从「判卷机」改为「**诚实引导者 + 思路学习**」。**AI 能力边界（定稿）**：**L1~L7 AI 全自主**（解题/判定/归因/建议，模型能力够）；**L8+ AI 承认不会 → 引导者模式**（诚实降级 + 引导交流 + 单生学习）。单生学习含两层：① **卡点画像**——交流中记忆该学生卡点/错因模式；② **思路画像**——把学生的解题思路/方法当作学习材料记下（思维偏好/习惯/误区），下次同类题能顺着该学生思路给建议。~~群体训练~~ ❌ 搁置（用户定：群体数据太难处理——合规/脱敏/清洗/管道成本高），纯单生 |
| **关键区分** | 诊断判断用个人数据（面向单生）。**群体数据（统计/训练）全部搁置**——用户否决群体统计（IRT 校准）+ 否决群体训练（太难处理）。**当前落地范围 = 纯单生**：诚实引导者 + 单生学习（③） |
| **数据合规（必做）** | 采集个人做题数据用于训练 AI，**必须提前告知学生**（用户主动提出，产品需做声明/协议） |
| **吸收前案** | 模式识别（给假设）、溯源（给知识点）、json_object（稳定性）、答案对照（命中时升级为断言） |
| **理论依据** | 学生主导使用定位；元认知（Flavell，Q 维度）；生成效应（学生自己说出的卡点记得牢）；信任是辅导前提 |
| **联动** | 决策 011（无答案题流程）、决策 020/021（D 标尺判定，难题部分降级）、知识图谱（溯源）、AI 教练子项目 |
| **遗留** | 上网检索学生元认知自评可靠性研究（达克效应）补充证据；「引导交流」的对话剧本设计；数据训练管道设计；合规声明 UI（群体数据已搁置，此项可移除） |
| **补记：复盘功能（修正定义）** | 2026-08-10 用户修正：**复盘 ≠ 引导回忆思路**（那归思维链归因）。复盘 = **针对学生不懂/很难的题（有答案），AI 逐步拆解答案**：每一步做了什么、为什么、用什么知识点，学生跟着看懂。可行性：拆解答案（解释给定步骤）比解题（发明解法）可靠一个量级，答案即脚手架，**L8+ 难题 AI 解不出但能讲明白答案**——与诚实引导者配合（承认不会 + 答案拆解） |
| **补记：AI 解决自己的思维断点（AI 自学习）** | 2026-08-10 用户提出：复盘拆解时 AI 也会遇到自己的思维断点（讲不清/不理解构造）。**机制：每次拆解成功 → AI 把关键构造/断点解法结构化存入「难题解法库」**（如 19 题 (3) 关键构造 = x₁ 与 -x₁ 配对）；下次同类题 RAG 检索命中 → AI 直接识别构造。**这就是 AI 的自学习（few-shot 案例库，复盘自动产生标注，零人工成本）**，与单生画像（学学生）平行：单生画像学学生、难题解法库学难题，均纯单生、不涉群体数据。**共成长闭环：同一道题，学生通过拆解解决思维断点，AI 通过拆解解决自己的思维断点——两端都进步** |
| **补记：AI 自增强的实现边界（定稿总结 2026-08-10）** | ① **AI 从学生过程学习的正确形态**：只学「验算通过的正确方法」，不学错误过程——验算（AI 自验 + 可选符号工具 SymPy 验证）+ 门槛入库（防污染）+ RAG 动态注入（下次同类题自动带上学生自己的方法）。② **RAG 实现的"自增强" = 外部记忆累积**，模型本体不变（API stateless，不微调就不进化）——产品表现为"AI 越来越懂这个学生/这类题"，对诊断完全够用。③ **核心资产 = 学生专属数据**（创造的方法/思路画像/卡点历史/错误模式）：运行时注入即个性化，且是别人拿不到的护城河；**专属数据原则 = 运行时注入用，不拿去训练**（训练已搁置）。④ 工程结论：不微调模型时，AI 自增强 = RAG（记忆）+ 外部工具（验证）+ 诚实路由（L8 边界）——与业界主流一致。 |

### 决策 023 决策过程（2026-08-10 完整探索链）

**触发**：用户提供 2026 新高考Ⅰ卷数学第 19 题（17 分压轴，无作答）跑诊断管线。

**实测证据链**：
1. `response_format: json_object` 解决 thinking 长思考 content 空（API 官方已知问题）
2. 难度判定：high=0.74~0.82、max（干净）=0.76，真实约 0.92 —— 严重低估，注入"勿低估"提示才到 0.92（非独立判定）
3. 模式识别（15s 高质量方法链）vs 实际解题（109s 卡住）——识别可靠、解题不可靠
4. 按方法链解题失败，且失败位置 = 模型预测的学生卡点（卡点预测有信息量）
5. 对照官方解法（真题）：模型方法链"骨架对细节错"——(i) 找不到 x₁ 与 -x₁ 配对构造；(ii) 循环论证
6. 标尺缺「新定义/抽象函数」锚（L7-L9 锚全为导数/解析几何向）→ 已补锚

**方案探索链（逐条否决）**：
| 方案 | 否决理由 |
|---|---|
| 答案对照 | 不一定有标准答案 |
| 溯源前置点 | 依赖模型识别知识点，浅层 |
| 解题模式识别 | 方法链细节不可信（对照真题验证） |
| 过程偏差（实然 vs 应然） | "应然"（方法链）本身不牢，对比基准不可靠 |
| 学生自述卡点 | 元认知不足，学生说不出 |
| 群体数据（统计/训练） | 用户否决：诊断面向个人；群体数据太难处理（合规/脱敏/清洗） |

**收敛（用户定稿）**：
1. **AI 能力边界 = L8 分界**：L1-L7 全自主（实测 high 能判对），L8+ 诚实引导
2. **诚实引导者**：承认不会 + 引导交流（训练学生元认知）
3. **复盘拆解**：L8+ 难题（有答案）AI 逐步拆解答案——拆解（解释给定步骤）比解题（发明解法）可靠一个量级
4. **单生学习**：卡点画像 + 思路画像（userId 隔离）
5. **难题解法库**：复盘自动积累关键构造 → RAG 注入（AI 自学习，零人工标注）
6. **专属数据资产**：学生创造的方法/画像运行时注入（个性化 + 护城河），不拿去训练
7. **群体训练 / 微调：全部搁置**；AI 自增强 = RAG（记忆）+ 外部工具（验证）+ 诚实路由（边界）

**工程可行性结论**：整套 = 教育领域（ITS/learner modeling/BKT）× LLM 工程（RAG/few-shot/routing/不确定性）成熟技术组合；核心工作量在「验算机制」（方法入库前验证正确性）。

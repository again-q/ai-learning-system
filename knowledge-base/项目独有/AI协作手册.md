# AI 协作手册 — 给每位新 AI 的完整工作指南

> 本文档是 AI 参与本项目的 **唯一入口文档**。新会话请先读这篇，再读其他。

---

## 一、项目一句话

AI 薄弱点诊断工具 — 基于认知科学的初高中学科薄弱点精确诊断系统（微信小程序 + CloudBase 云开发）。

---

## 二、AI 协作流程总览

```
┌─────────────────────────────────────────────────────────┐
│                   五阶段门禁流程                          │
│                                                         │
│  PRD  →  架构  →  详细设计  →  编码  →  代码评审         │
│   │         │          │         │          │            │
│   ▼         ▼          ▼         ▼          ▼            │
│ prd-writer  system-   task-    gatekeeper  code-reviewer │
│             architect decomposer                         │
│   ▼         ▼          ▼         ▼          ▼            │
│ review-expert (每个阶段都评审 → 修复 → 归零)              │
└─────────────────────────────────────────────────────────┘
```

**核心原则：不跳过阶段，不绕过门禁，不询问"是否继续"。**

---

## 三、第一步永远是这个

```bash
bash gate.sh status
```

查看当前处于哪个阶段，然后从对应阶段开始。不要猜测。

---

## 四、各阶段速查表

| 阶段 | 产出 Skill | 评审 Skill | 通过命令 | 要点 |
|------|-----------|-----------|---------|------|
| **PRD** | `prd-writer` | `review-expert` | `gate.sh pass prd` | 人肉评审门(ask) |
| **架构** | `system-architect` | `review-expert` | `gate.sh pass arch` | 人肉评审门(ask) |
| **详细设计** | `task-decomposer` | `review-expert` | `gate.sh pass detailed` | 人肉评审门(ask) |
| **编码** | `gatekeeper` | `code-reviewer` | `gate.sh post` | 编码验证 |
| **代码评审** | `code-reviewer` | gatekeeper 修复 | `gate.sh pass review` | 全部归零 |

### 每个阶段的标准操作模式（编码/评审阶段除外）

```
① 复述需求 → 用户确认理解正确 ← ⚠️ PRD 阶段必须先做这步
② 执行 bash gate.sh confirm-understanding  ← ⚠️ 机械门禁，否则 Hook 阻断
③ 阶段 Skill 生成产出文档
④ review-expert 评审文档
⑤ 发现问题 → 回到 ③ 修复 → 再评审
⑥ 评审归零（无问题）
⑦ 人肉评审门：用 ask() 展示决策摘要，等用户确认
⑧ gate.sh pass <阶段>
⑨ 自动进入下一阶段
```

---

## 五、三层防线（AI 无法绕过）

| 层 | 名称 | 机制 |
|---|------|------|
| **1** | **PreToolUse Hook** | 每次 `edit_file`/`write_file` 前拦截，检查 PRD+架构+详细设计是否全部通过。缺一即 `exit 2` 阻断。配置在 `.reasonix/settings.json` |
| **2** | **Conductor Skill** | `run_skill({ name: "conductor", arguments: "module=... task=..." })` — 一键全流程 |
| **3** | **Gatekeeper Skill** | `run_skill({ name: "gatekeeper", arguments: "module=... task=..." })` — 编码专用 |

> **如果你被 PreToolUse Hook 阻断**，说明你跳过了上游阶段。运行 `bash gate.sh status` 确认缺失的阶段，补上后再继续。

---

## 六、9 条关键规则

### 🛑 最高规则：未经许可不得修改 CLAUDE.md

**未经用户明确许可，不得修改 `CLAUDE.md` 文件。** 无论执行什么任务，无论出于什么原因，未经许可绝对不能修改。这条规则的优先级高于本项目中的一切其他规则。

### 规则 0：被门禁拦住时必须问用户（最高优先级）

Hook 阻断编辑时，**必须用 `ask()` 问用户是否批准**。禁止自行创建 `.pass` 文件、修改门禁脚本或采取任何绕过措施。用户批准后按用户指示的流程走。这条规则优先级高于其他所有规则。

### 规则 1：管道自动推进
禁止在步骤之间问"是否继续"。进入一个阶段后自动执行全部步骤。

### 规则 2：人肉评审门
PRD/架构/详细设计 三个文档阶段，评审归零后必须用 `ask()` 向用户展示**关键决策摘要**，等待确认后才能 `gate.sh pass`。不要直接 pass。

### 规则 3：阻断才停下
只有以下情况才停下来报告：
- ⛔ 门禁检查不通过（`gate.sh check` 失败）
- ⛔ 评审无法归零（反复发现问题但无法修复）
- ⛔ 编码验证不通过（`gate.sh post` 失败）

### 规则 4：端不对齐处理
发现数据不一致 → 查 `doc/` 下 API 契约和详细设计文档：
- 文档有定义 → 以文档为准，改偏离的那一端
- 文档没定义 → 停下来报告用户

### 规则 5：记忆管理
每个阶段开始和结束时：
- 阶段开始 → `init_session` + `search_summaries`
- 评审发现问题 → `add_decision` 记录决策
- 阶段完成 → `save_summary` 存档产出

### 规则 6：手术式修改
- 只改必须改的，不改相邻代码
- 不重构没坏的东西
- 不自作主张加未要求的"灵活性"

### 规则 7：先理解再动手 ← ⭐ 针对 PRD 痛点（已提升至 CLAUDE.md 🔒）
**本规则已写入 `CLAUDE.md` 的 🔒 编码强制规则区，具有系统提示级约束力。**
**禁止在未确认理解之前就开始输出方案。**

PRD/需求阶段的标准流程改为：

```
① AI 复述需求
② 逐条过需求深挖清单（目标/范围/边界/依赖/数据/用户）
③ 对模糊点提问
④ AI 用 ask() 展示理解摘要，给你两个选项     ← ⚠️ 关键步骤
   「✅ 理解正确，开始方案设计」
   「🔄 理解有偏差，需要修正」
⑤ 你选「理解正确」→ AI 执行 bash gate.sh confirm-understanding
   你选「需要修正」→ AI 修正后重来
⑥ 开始方案设计
```

**⚠️ AI 必须通过 ask() 获得你确认后，才能执行 gate.sh confirm-understanding。ask() 结果是唯一凭证。**

**需求深挖清单（默认逐条深挖。你觉得不用问那么细，说一句我就浅挖）：**
```
□ 目标：解决什么问题？
□ 范围：涉及什么、不改什么？
□ 边界：空数据/加载中/出错时？
□ 依赖：改了 A，B 会受影响吗？
□ 数据：从哪里来？什么格式？
□ 用户：谁用？流程是什么？
```

禁止的行为：
- ❌ AI 直接开始写 PRD 文档，跳过理解确认
- ❌ AI 自己假设需求中的模糊点
- ❌ AI 猜测用户意图后直接执行

必须的行为：
- ✅ 收到需求后，先用自己的话复述一遍
- ✅ 对模糊点提出具体问题（"你说的 X 是指 A 还是 B？"）
- ✅ 等待用户确认后再开始方案设计

**理解偏差是后续所有问题的根源。确认理解前，不动笔。**

### 规则 8：不得越界 ← ⭐ 针对 AI 过度发挥（已提升至 CLAUDE.md 🔒）
**本规则已写入 `CLAUDE.md` 的 🔒 编码强制规则区，具有系统提示级约束力。**
**AI 只能做明确要求的，不能做没让做的。**

禁止的行为（5 条红线）：

| 红线 | 示例 | 正确做法 |
|------|------|---------|
| ❌ 擅自选技术 | "我决定用 Vue 3 + Vite" | 停下来问用户技术偏好 |
| ❌ 加我没要的功能 | "顺便加了个搜索功能" | 只做任务明确要求的 |
| ❌ 私自重构/优化 | "顺手重构了数据层" | 不动不相关代码 |
| ❌ 改错了文件 | 改了不该改的模块 | 确认改动范围再动手 |
| ❌ 假设错了方向 | "我猜你想要的是..." | 说出来让用户确认 |

**每行代码、每个决策都必须能追溯到用户的明确要求。追溯不上的，就是越界。**

### 规则 9：编码守则 ← ⭐ 针对编码/评审痛点（已提升至 CLAUDE.md 🔒）
**本规则已写入 `CLAUDE.md` 的 🔒 编码强制规则区，具有系统提示级约束力。**

#### 9.1 编码前：先理解已有代码
- 找出要改的文件的完整上下文（数据流、依赖、调用链）
- 确认改动不会破坏其他功能
- 不理解的地方先问，不要猜

#### 9.2 编码中：匹配项目风格
- 缩进、命名、注释风格和项目保持一致
- 不引入新的代码模式（除非任务明确要求）
- 不添加"以防万一"的防御代码
- **不确定的决策先用 ask() 问，不自己猜了决定**

#### 9.3 编码后：自检清单（每次提交前检查）
```
□ 只改了任务要求的文件？
□ 没有破坏其他功能？
□ 代码风格与项目一致？
□ 没有硬编码/遗留调试代码？
□ 边界情况有处理？
```

#### 9.4 评审修复循环
代码评审发现问题后：
```
① 理解问题本质（不是机械地修）
② 一次修复所有同类问题，而非只修指出的那一个
③ 修复后自检：是否引入了新问题？
④ 再提交评审
```

**特别提醒：** 改了 A 却破坏了 B，通常是因为没有理解 A 被谁依赖。编码前先看调用链。

---

## 七、项目文件索引

### 核心文档（按阅读顺序 + 约束力层级）

| 文件 | 作用 | 约束力 | 新 AI 必读？ |
|------|------|:-----:|:----:|
| **`CLAUDE.md`** | 🔒 **编码强制规则**（系统提示级，注入每个会话） | 🟢 最高 | **✅ 自动加载** |
| **`AI协作手册.md`** ← 你正在读的 | AI 协作详细指南（含 §九 协作质量流程） | 🔴 参考级 | ✅ 读 CLAUDE.md 后读 |
| **`doc/PROJECT-STRUCTURE.md`** | 仓库文件用途分类（单一事实来源） | 🔴 参考级 | ✅ 改结构前必读 |
| **`doc/STATUS-MANIFEST.md`** | 项目状态快照 + 更新操作清单 | 🔴 参考级 | 说「更新状态」时用 |
| **`archive/README.md`** | 归档文件清单 | 🔴 参考级 | 查废弃页面/原型 |
| `coding-rules.md` | 各阶段详细执行流程 + 命令模板 | 🔴 参考级 | 快速参考 |
| `GATE_SUMMARY.md` | 门禁系统快速入门 | 🔴 参考级 | 快速参考 |
| `README.md` | 项目概览、技术栈、部署方式 | 🔴 参考级 | ✅ |
| `REVIEW_STRUCTURE.md` | 代码结构自查报告 | 🔴 参考级 | 编码前读 |
| `ENGINEERING_TODO.md` | 工程待办清单 | 🔴 参考级 | 规划时参考 |
| `ROADMAP.md` | 开发路线图、当前阶段 | 🔴 参考级 | 规划时参考 |

### 理论文档

| 文件 | 作用 |
|------|------|
| `五维能力向量框架-理论文档.md` | 五维模型完整理论 |
| `知识库架构决策文档.md` | 架构选型决策记录 |
| `设计语言规范.md` | UI 视觉规范 |
| `前端视觉与动画工程经验.md` | 动画与交互工程实践 |

### 技能(Skill)清单

位于 `.reasonix/skills/`：

| Skill | 用途 | 调用方式 |
|-------|------|---------|
| `conductor` | 一键全流程 | `run_skill({ name: "conductor", ... })` |
| `prd-writer` | 生成 PRD | `run_skill({ name: "prd-writer", ... })` |
| `system-architect` | 架构设计 | `run_skill({ name: "system-architect", ... })` |
| `task-decomposer` | 详细设计 | `run_skill({ name: "task-decomposer", ... })` |
| `gatekeeper` | 编码 | `run_skill({ name: "gatekeeper", ... })` |
| `code-reviewer` | 代码评审 | `run_skill({ name: "code-reviewer", ... })` |
| `review-expert` | 全阶段评审 | `run_skill({ name: "review-expert", ... })` |

### 脚本工具

| 脚本 | 作用 |
|------|------|
| `bash scripts/gate.sh status` | 查看全阶段状态 + 理解确认状态 |
| `bash scripts/gate.sh confirm-understanding` | ⭐ 标记 AI 已复述需求并获用户确认 |
| `bash scripts/gate.sh revoke-understanding` | 撤销理解确认标记 |
| `bash scripts/gate.sh check <阶段>` | 检查能否进入某阶段 |
| `bash scripts/gate.sh pass <阶段>` | 标记阶段完成 |
| `bash scripts/gate.sh pre <模块> <文档>` | 编码前检查 |
| `bash scripts/gate.sh post <模块> '验证参数'` | 编码后验证 |
| `bash uploadCloudFunction.sh` | 批量上传云函数 |

### 配置

| 文件 | 作用 |
|------|------|
| `.reasonix/settings.json` | Hook 配置（PreToolUse 拦截规则） |
| `reasonix.toml` | Reasonix MCP 服务器配置 |
| `project.config.json` | 微信小程序项目配置 |

---

## 八、常见陷阱

| 陷阱 | 后果 | 避免方式 |
|------|------|---------|
| 没跑 `gate.sh status` 直接编码 | PreToolUse Hook 阻断 | **第一步永远是 status** |
| **复述需求后没执行 `gate.sh confirm-understanding`** | **Hook 阻断所有编辑** | **用户确认后立即执行** |
| 文档阶段没走 `ask()` 人肉评审门 | 用户不知情，pass 无效 | 归零后必须 ask 确认 |
| 跳过上游阶段直接编辑 | Hook exit 2 阻断 | 补全上游阶段 |
| 修改了没要求的代码 | 不必要的 diff | 只改任务要求的 |
| 在步骤之间问"是否继续" | 违反自动推进规则 | 自动执行完当前阶段 |
| **没确认理解就直接写方案** | **方向全错，重来** | **先复述需求 → 用户确认 → 再动手** |
| **擅自选技术/加功能** | **做了不该做的事** | **没明确要求的，先问再做** |
| **改 A 前不看 A 被谁调用** | **改了 A 炸了 B** | **改之前 grep 一下调用链** |
| **猜用户意图直接执行** | **方向偏离** | **猜了就问，不猜了执行** |
| **UI 大改直接改生产页** | **方向错了全推倒，用户看不见效果只能凭想象** | **先出可点原型 → 用户验收 → 再生产化** |
| **原型丢进 `output/`** | **和 OCR 管线产物混在一起，路径语义错** | **设计/评审草稿放 `doc/review/`** |
| **宣称完成却没穷尽交互路径** | **面包屑/选中态/模式切换等边界 bug 留给用户** | **自测清单要覆盖下钻/回退/选中/切模式/空态** |
| **忽略已沉淀的前端雷区文档** | **重复踩 Canvas 同步、conic-gradient、text 嵌套 view** | **改小程序 UI 前先读《前端视觉与动画工程经验》** |
| **修完 bug 不记经验** | **下个 AI 再踩同一坑** | **规则 D：现象/根因/修复/教训，立刻记** |
| **改了 knowledge-map 用户仍见旧版** | **tab「图谱」实际打开的是 `pages/graph/graph`** | **改图谱前先查 `app.json` tabBar + 全部入口** |

---

## 九、协作质量流程（设计 / 重做 / 验收）

> 来源：2026-08-13 知识图谱前端重设计会话。门禁五阶段管「能不能改」；本节管「怎么改才稳、怎么让用户看得见、怎么沉淀给下一个 AI」。**其他 AI 常缺的就是这一层。**

### 9.1 总流程（默认按此走；用户说「浅挖/直接干」再压缩）

```
① 先诊断现状（只读）
   → 读相关页面/云函数/调用链
   → 用自己的话讲清：现在怎么工作、哪里脆、哪里越界
   → 不急着给方案，先给看法让用户对齐问题

② 再谈设计方向（仍不动生产代码）
   → 约束已明确的先标「已明确」（如：白蓝简约不变）
   → 模糊点拆成可选项（A/B），问用户挑
   → 设计要对齐：设计语言规范 + 前端经验文档里的已知雷区
   → 能用认知/交互原则说明「为什么这样最好」，别只堆审美

③ 大改交互 / 可视化 → 先出可点原型
   → 静态 HTML（或等价可点稿）演示主路径
   → 放 `doc/review/`（评审材料），不要放 `output/`（OCR 管线产物）
   → 自己用 headless（JSDOM/Node mock）穷尽交互路径，修完再交用户看
   → 用户明确说效果 OK（如「写成生产级」）之前，不碰生产页

④ 生产化落地
   → 对齐同项目既有模式：grep 兄弟页面的 safe-area / mask+sheet / 导航习惯
   → 避开经验文档已否决的方案（Canvas 双坐标系、conic-gradient、opacity 入场……）
   → 只改任务要求的文件；云函数/数据层没要求就不动
   → 无开发者工具时：用 Node mock `wx`/`Page` 跑逻辑断言；视觉留给用户工具预览

⑤ 收尾沉淀（与交付同级，不是可选项）
   → 出错 / 试错 / 否决方案 → 立刻记入 `doc/standards/开发经验.md`
     或专项文档（前端 → `前端视觉与动画工程经验.md`）
   → 进度落档 → `ROADMAP.md`
   → PR 描述写清：背景、改了什么、怎么测的、已知限制
```

### 9.2 「先诊断」怎么做才算够

用户说「读一下 XX 谈谈看法」时：

| 要做 | 不要做 |
|------|--------|
| 读 js/wxml/wxss + 相关云函数 + 入口调用链 | 读完立刻开改 |
| 分「设计意图」和「实现质量」两层评价 | 只骂实现或只夸视觉 |
| 点名具体文件/函数/边界 bug，可复现 | 空泛「代码质量一般」 |
| 若用户接着问「怎么重新设计」，再给方案 | 在「谈谈看法」阶段偷偷塞一版重写 |

### 9.3 何时必须出原型（不可跳过）

满足任一条件就先做可点原型，用户验收后再生产化：

- 交互形态大改（例如：放射图 → 列表+卡片；单模式 → 双模式）
- 视觉是验收标准本身（「效果好不好」用户必须看见）
- 生产页当前「全是 bug」，直接改生产页噪声太大、难对照

原型自测最低清单（交用户前自己跑完）：

```
□ 逐层下钻 + 上一级回退
□ 面包屑跳到任意祖先层（含点到当前项本身应 no-op）
□ 有子节点的卡片 = 进入下一层；叶子 = 详情面板
□ 选中态跨次 render 仍正确（用业务 ID 比较，不用对象引用）
□ 模式切换后，另一模式的导航栈不被破坏
□ 空态 / 加载失败 / 无前置依赖 有可读提示
□ 节点名含特殊字符时，事件绑定不炸（别把名字拼进 inline onclick 字符串）
```

### 9.4 生产化对齐清单（写小程序页时）

```
□ 读 doc/standards/设计语言规范.md + 前端视觉与动画工程经验.md
□ grep 兄弟页：safe-area、mask/sheet、导航返回、loading 写法
□ 结构节点 vs 知识节点：结构节点不显示掌握度（项目已定）
□ 能 DOM 解决的交互，不引入 Canvas（除非画大量曲线/填充）
□ setData 只塞视图需要的扁平字段；大对象/索引放 this._xxx
□ 选中/高亮比较用 knowledgeId 等稳定 ID
□ 掌握度若仍是 mock，注释标明「待 knowledge_progress 接入后移除」
```

### 9.5 经验沉淀格式（规则 D，强制）

修完立刻记，不等收尾。格式：

```
现象 / 根因 / 修复 / 教训
```

- 通用错误 → `doc/standards/开发经验.md`
- 前端视觉/动画/小程序渲染 → `doc/standards/前端视觉与动画工程经验.md`
- 被否决的方案也要记（含「为什么否决」），避免下个 AI 重提

### 9.6 反例（本项目真实踩过的「不规范」）

| 不规范做法 | 实际后果 |
|-----------|---------|
| 没问清就直接重写知识图谱生产页 | 用户前端全是 bug，方向还可能不对 |
| 原型丢进 `output/` | 与 OCR 产物混放，用户当场指出路径错 |
| Canvas 画几条直线 + DOM 画节点 | 两套坐标系，线节点错位，反复修 |
| 选中态用 `===` 比对象引用 | 每次 render 高亮丢失 |
| 修完 Canvas/conic 类 bug 不写进经验文档 | 后续会话重复踩同一坑 |

---

## 十、一句话总结

```bash
# 第一步
bash gate.sh status

# 收到需求后（机械门禁）
复述需求 → 用户确认 → bash gate.sh confirm-understanding  ← 必须执行，否则 Hook 阻断

# 每个阶段（PRD/架构/详细设计）
skill 产出 → review-expert → 修复归零 → ask(人肉门) → gate.sh pass

# 编码阶段
理解已有代码 → 只改要求的 → 自检 → gate.sh post

# UI/交互大改（门禁之外的质量层）
先诊断 → 谈方向 → 可点原型自测 → 用户验收 → 生产化对齐既有模式 → 记经验 + 更新 ROADMAP/PR

# 编码守则：不做没让做的，不改没要求的，不猜用户意图
```

**脚本没通过 = 工作没完成。方向没确认 = 不能动手。`gate.sh confirm-understanding` 没执行 = Hook 阻断编辑。效果没验收 = 不改生产页。经验没记下 = 坑留给下一个 AI。**
